"""Fixed infrastructure for the idea-to-evidence pipeline (DESIGN.md).

Agents specify; this package executes. Data access, portfolio construction,
costs, backtesting and gates live here as deterministic code that agents call
but cannot modify (DESIGN §1).
"""
